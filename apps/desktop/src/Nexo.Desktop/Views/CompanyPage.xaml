<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Nexo.Desktop.ViewModels"
             x:Class="Nexo.Desktop.Views.CompanyPage"
             x:DataType="vm:CompanyViewModel"
             Title="Administrar Empresa">

    <RefreshView IsRefreshing="{Binding IsRefreshing}"
                 Command="{Binding RefreshCommand}">
        <VerticalStackLayout Spacing="12" Padding="16">
            <Label Text="Empresas" Style="{StaticResource Headline}" />

            <CollectionView ItemsSource="{Binding Companies}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="x:Object">
                        <Frame Style="{StaticResource Card}" Margin="0,4">
                            <HorizontalStackLayout Spacing="12">
                                <Label Text="🏢" FontSize="24" VerticalOptions="Center" />
                                <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                                    <Label Text="{Binding Name}" FontSize="15" FontAttributes="Bold" />
                                    <Label Text="{Binding InviteCode, StringFormat='Código: {0}'}"
                                           FontSize="11" TextColor="{StaticResource TextSecondary}" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        </VerticalStackLayout>
    </RefreshView>
</ContentPage>
