<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Nexo.Desktop.ViewModels"
             x:Class="Nexo.Desktop.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Surface}">

    <ScrollView>
        <VerticalStackLayout Spacing="16" Padding="32"
                             VerticalOptions="Center"
                             MaximumWidthRequest="400">
            <Label Text="Nexo" Style="{StaticResource Headline}"
                   HorizontalOptions="Center" TextColor="{StaticResource Primary}" />
            <Label Text="Iniciar Sesión" Style="{StaticResource Subheadline}"
                   HorizontalOptions="Center" Margin="0,0,0,24" />

            <Entry Placeholder="Correo electrónico"
                   Text="{Binding Email}"
                   Keyboard="Email" ReturnType="Next" />

            <Entry Placeholder="Contraseña"
                   Text="{Binding Password}"
                   IsPassword="True" ReturnType="Done" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="{StaticResource Danger}"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                   FontSize="13" />

            <Button Text="Ingresar"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryButton}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
