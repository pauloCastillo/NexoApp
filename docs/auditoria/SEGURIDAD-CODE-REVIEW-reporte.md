# Auditoría Code Review — Correctness / Security / Performance — NexoApp

**Rama:** `01-Auditoria-del-codigo` | **Fecha:** 2026-08-31 | **Agente:** `@Code Reviewer` + `@agente-grasp`/`@agente-solid` guardrail `depth=1` | **Scope:** `server/` + `app/lib/` | **Modo:** solo reporte

> No duplica GRASP/SOLID. Si solapa, referencia como `[SOLID-...]`.

| Categoría | Sev | Archivo:Línea | Por qué | Sugerencia |
|---|---|---|---|---|
| Seguridad — Geofence bypass | Crítica | `server/src/utils/geofence.ts:30-31` | `if(candidates.length===0) return {inside:true}` → empresa sin sucursales ni `company.location` queda con geocerca abierta. Cualquier coordenada spoofeada marca como dentro. | Retornar `{inside:false}` y rechazar en `locationRepository.ts:47` o exigir al menos 1 sucursal activa en `branchesController.ts:40`. Ref `RN-005`. |
| Seguridad — Tenant isolation incompleto | Crítica | `server/src/routes/work-orders.ts:1-22`, `server/src/routes/employees.ts:14-25`, `server/src/middlewares/tenantGuard.ts:38` | `requireCompanyAccess` solo montado en `branches`. `employees`, `work-orders`, `locations` confían en `companyId` JWT pero nunca validan `targetCompany` no inyectado vía `req.body.company`/`query.company` | Middleware global `verifiedToken`+`requireCompanyAccess` en `main.ts:54` con allowlist (`/api/auth/*`, `/api/health`, `GET /invitations/validate`). Eliminar `company` del body en controllers. |
| Seguridad — CORS abierto por defecto | Crítica | `server/main.ts:31-32` | `allowedOrigins.length>0 ? allowedOrigins : true` → si `CLIENT_URL` no definido, `cors({origin:true, credentials:true})` refleja cualquier Origin | `origin: allowedOrigins ?? []` + `credentials:false` por defecto; fallar arranque si `!isDev && !CLIENT_URL`. Igual en `socketManager.ts:12`. |
| Seguridad — JWT ventana de robo | Crítica | `server/src/utils/utils.ts:31-43`, `server/src/services/authService.ts:158-206` | `signSession` 24h + `signRefreshToken` 7d en `localStorage`/`SecureStorage` sin httpOnly, sin rotación corta, sin revocación global (`logout` solo borra hash actual). `verifyingSession` traga error y retorna `{error}` sin throw | Access 15min httpOnly `Secure` cookie, refresh rotativo con `jti` + tabla revocación; `verifyingSession` debe `throw`. Añadir `helmet` CSP y `express.json({limit:'100kb'})`. |
| Correctness — Registro sin transacción | Alta | `server/src/services/authService.ts:29-35`, `server/src/services/authService.ts:96-111` | `Company.create` + `User.create` no atómicos (sin `mongoose.startSession`). Si `User.create` falla, queda empresa huérfana. Regex `^${name}$` sin índice collation → race duplicados | `session.withTransaction(async()=>{Company.create(...{session}); User.create(...{session})})` + índice único `Company(name)` con `collation:{locale:'es',strength:2}`. |
| Correctness — Race transición órdenes | Alta | `server/src/services/workOrderService.ts:38-65` | `getById` → chequea `VALID_TRANSITIONS` → `updateWorkOrder`. TOCTOU: dos `PATCH /:id/start` concurrentes ambos ven `pendiente` y ambos escriben `en_progreso` | `findOneAndUpdate({_id:id, company:ctx.companyId, status:{$in: VALID_TRANSITIONS[old]}}, {$set:{status:new}}, {new:true})` atómico; si `null` → 409. |
| Correctness — TimeControl hijack | Alta | `server/src/repositories/timeControlRepository.ts:15-27` | `createTimeControl` usa `findByIdAndUpdate(id, {$set:{company}})` sin filtrar `company`. IDOR: employee de empresa A sobrescribe `ControlTime` de empresa B | `findOneAndUpdate({_id:id, company:context.companyId}, updateData, {new:true})`; si null → 404. |
| Seguridad — Socket sin autenticación | Alta | `server/src/routes/locations.ts:17-44`, `server/src/routes/employees.ts:33-68`, `server/main.ts:95-97` | Namespace `/api/locations` y `/api/dashboard` no verifican JWT (`employeeNamespace.use` solo asigna requestId). Cualquier cliente emite `getLocation` y escucha cross-tenant | `io.of('/api/dashboard').use(socketAuth(verifyingSession))`; rechazar sin token. Flutter `dashboard_socket_service.dart:12` enviar token obligatorio. |
| Correctness/Validación — Rutas sin Zod | Alta | `server/src/routes/work-orders.ts:16`, `server/src/routes/locations.ts:15`, `server/src/controllers/locationController.ts:25` | Solo `notifications.ts:9` y `branchesController.ts:35` usan `validate()`/`safeParse`. `POST /api/locations`, `POST /api/employees`, `POST /api/work-orders` aceptan `req.body` crudo → NoSQL injection, lat/lng no validados | Añadir `validate(locationTimeSchema)` y `validate(createWorkOrderSchema)` en routes; Zod `lat: z.number().min(-90).max(90)`. |
| Seguridad/Performance — DoS y log de secretos | Alta | `server/main.ts:33`, `app/lib/core/network/dio_client.dart:26` | `express.json()` sin `limit` → payload 50MB bloquea event-loop. `LogInterceptor(requestBody:true)` en release loguea `access_token`, PII en logcat | `express.json({limit:'100kb'})`; `LogInterceptor` solo bajo `kDebugMode` o `assert`. |
| Seguridad — Geofence override sin audit fuerte | Media | `server/src/repositories/locationRepository.ts:47-54` | `override:true` permitido a `supervisor/admin/business_owner` sin límite frecuencia ni `overrideReason` validado (puede ser vacío). No se audita como alerta | Exigir `overrideReason: z.string().min(10)` y rate-limit 3/día/usuario; `auditLogService.log({action:'geofence.overridden'})` + alerta Socket a `business_owner`. |
| Performance — Geocoding cuello de botella | Media | `server/src/utils/geocoding.ts:3-42` | `Map` cache sin evicción + `queue` serial global + `throttle 1100ms` → 1 req/s para todo el cluster, bloquea `POST /api/locations` 5s bajo carga | `lru-cache@11` con `max:2000, ttl:5min` + `p-limit(5)` paralelo; mover `reverseGeocode` fuera del path crítico (fire-and-forget). |
| Correctness — Offline replay inseguro | Media | `app/lib/core/storage/pending_queue.dart:12-21`, `app/lib/core/storage/sync_service.dart:7-15` | `PendingQueue` guarda en `SecureStorage` y `SyncService.syncAll` hace `dio.post('/locations', items[i])` sin re-evaluar geofence, sin `companyId`, y `catch(_){}` silencia errores → reenvía ubicaciones spoofeadas antiguas | Re-validar geofence server-side + `queuedAt` expiración 24h y `maxRetries:3`; no silenciar: `if 4xx remove, if 5xx retry`. Usar Hive cifrado para cola. |
| Testing — Flujos críticos sin cobertura | Alta | `server/src/__tests__/` | Solo `utils/geofence/normalizeError/proxyController`. Cero tests para `authService.registerOwner/registerUser`, `locationRepository.createLocation`, `workOrder` transiciones concurrentes, `tenantGuard.requireDeptScope`, `socketManager` | Añadir `jest` + `mongodb-memory-server` para: invitación single-use atomic, geofence open→closed, workOrder race, tenant cross-company 403. Mín 1 test por Sev Crítica. |
| Seguridad — Invitaciones sin límite creación | Media | `server/src/routes/invitations.ts:14-21`, `server/src/routes/invitations.ts:24` | `validateLimiter` 10/min solo en `GET /validate/:code`, pero `POST /` y `POST /request-new` sin limiter → spam invitaciones / enumeración códigos (12 hex chars = 48 bits) | `rateLimit({max:20/hour})` en `POST /` + `POST /request-new`; añadir CAPTCHA turnstile en `request-new`; índice parcial `isActive:true`. |

## Priorización
Corregir 1-4 antes de próximo deploy; 5-8 en mismo sprint; 9-15 deuda planificada.
